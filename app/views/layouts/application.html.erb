<!DOCTYPE html>
<html>
  <head>
    <title><%#= full_title(yield(:title)) %>Iscube Blog</title>
	<%= javascript_include_tag "jquery-rails.js"%>
		<%= javascript_include_tag "jquery-1.8.0.min"%>
		<%= javascript_include_tag "jquery-ui-1.8.23.custom.min"%>
		<%= javascript_include_tag "bootstrap.min"%>
		<%= javascript_include_tag "bootstrap"%>
		<%= javascript_include_tag "staff"%>
   <%= stylesheet_link_tag('sblog','jquery-ui-1.8.23.custom.css','bootstrap.min','bootstrap','bootstrap-responsive','bootstrap-responsive.min', :media => "all") %>
		
    
    <%= csrf_meta_tags %>
  </head>
  <body>
	<div id="topbar">
	
	<%= render 'layouts/header' %>
	 <%= render 'layouts/flash_messages' %>
					
	</div>
		</br></br></br>
			<h1><%= @page_title %></h1>
			
				<% if !flash[:notice].blank? %>
				<div class="notice">
				 <%= flash[:notice]%>	
				</div>	
				<% end %>
					
					<%= yield %>
		
		
	</body>
</html>